# Tasks 1: JSON Task Manager Prototype

This is the initial command-line application prototype, storing tasks in a simple `tasks.json` file as required by the milestone.

## Usage 

Ensure you are in the `tasks1` directory.

### 1. Add a Task
```bash
python task_manager.py add "Draft the Tasks 2 SQLite refactor."